# nmap-auto-parser
A Python CLI tool that runs Nmap, saves the output in XML, and immediately parses it so you don’t have to touch XML at all.  Scan first, understand later — automatically.  Built for people who like results more than files.
